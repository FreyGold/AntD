import { Checkbox as AntCheckbox } from "antd";

const Checkbox = (props: any) => (
   // TODO: add api call to mutate the checked property
   <AntCheckbox {...props}></AntCheckbox>
);

export default Checkbox;
